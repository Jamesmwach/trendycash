import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  server: {
    port: 3000, // Optional: Define a port for local development
  },
  build: {
    // Optional: Add build-specific options if needed in the future
  },
});
